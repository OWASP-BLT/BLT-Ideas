# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real tokens.

# GitHub Personal Access Token (classic or fine-grained, read:org + public_repo scopes)
# Without this, the generator runs but skips live contributor data from the GitHub API.
GITHUB_TOKEN=your_personal_access_token_here
